rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Public logo assets; uploads restricted to signed-in admins
    match /logos/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}